apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: activity-local-claim
  namespace: volume-activity
spec:
  storageClassName: activity-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
